# The main pipeline for youtube_project_bundle

resources:
  pipelines:
    bronze_silver_earthquake_etl:
      name: Bronze_Silver_EarthQuake_Prod
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/DLT_pipelines/Bronze_Silver_EarthQuake/transformations/**
      schema: silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/DLT_pipelines/Bronze_Silver_EarthQuake


